spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/test-db
    username: root
    password: 6548
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

