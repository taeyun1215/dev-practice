server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/JPA_N+1_ISSUE
    username: root
    password: 6548
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
  logging:
    level:
      org:
        hibernate:
          SQL: DEBUG
          type:
            descriptor:
              sql: TRACE